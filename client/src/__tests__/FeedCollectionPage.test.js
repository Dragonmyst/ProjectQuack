import { render, screen } from '@testing-library/react';
import FeedCollectionPage from '../pages/FeedCollectionPage';
import { act } from 'react-dom/test-utils';

test('renders title', () => {
    // render(<FeedCollectionPage />);
});

test('renders inputs', () => {

});

test('renders validation errors', () => {

});

test('renders toast on success', () => {

});

test('renders toast on error', () => {

});

test('handles submitting data', () => {

});